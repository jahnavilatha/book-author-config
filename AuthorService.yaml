spring:
  #  config:
  #    import: optional:configserver:http://localhost:8888
  application:
    name: AuthorService
#  cloud:
#    config:
#      uri: http://localhost:8888
#      name: Author-service
server:
  port: 8080
eureka:
  client:
  service-url:
  defaultZone: http://localhost:8761/eureka/
springdoc:
  api-docs:
    path: /api   #http://localhost:8080/api
  swagger-ui:
    path: /swagger-ui-custom.html #http://localhost:8080/swagger-ui/index.html